{
  "name": "game-knywong-functions",
  "description": "Firebase Functions for game.knywong.com activities",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "pdfkit": "^0.15.2"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.13.9",
    "typescript": "^5.0.0"
  },
  "private": true
}
